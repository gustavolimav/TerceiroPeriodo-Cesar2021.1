# Produto Matricial

## Programa para multiplicar duas matrizes de tamanho indefinido
## introduzidos pelo usuário em dois arquivos distintos.

SOBRE

* Conteúdos:
         fun.c, fun.h, makefile and mult_glvm.c.

* Como usar:  
         1. Crie ou coloque dois arquivos que contenham matrizes na pasta
         2. Utilize o comando "make".
         3. Utilize o comando "./mult [file1] [file2]" com o nome dos dois arquivos que você criou
         4. Caso não as matrizes sejam válidas, abra o arquivo "mult_glvm.out" para ver o resultado.
         5. Limpe todos os arquivos criados com "make cleanAll" ou todos menos o "mult_glvm.out" com o comando "make clean".

   * Pre Requisitos: 
         gcc e make.

* Tecnologias:
         C e Makefile.

fun.c:
    Arquivo com as funções que o "mult_glvm.c" utilizará.

fun.h:
    Arquivo que faz a ligação entre o "fun.c" e o "mult_glvm.c" inicializando as funções
    e incluindo as bibliotecas que serão utilizadas.

Makefile:
    Arquivo que compila o programa utilizando o comando "make" e limpa utilizando "make clean" ou o "make cleanAll".

mult_glvm.c:
    Arquivo principal que executa o Poduto Matricial.

mult_glvm.out:
    Arquivo que será gerado se as matrizes estiverem corretas e puderem ser multiplicadas.
    O Conteúdo desse arquivo vai ser o output do programa "mult_glvm.c".

